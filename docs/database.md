# Database

> In the context of database technology,
> facing the need to choose what to support
> I use `pgx` with PostgreSQL,
> over `database/sql` and supporting multiple dbs like sqlite,
> to achieve simple code and not being limited to the least common denominator,
> accepting that there will be hard database lock-in.
